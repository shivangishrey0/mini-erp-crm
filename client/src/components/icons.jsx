// Re-exports lucide-react icons under the names already used across the app,
// so swapping the hand-rolled SVG set for a real icon library didn't require
// touching every import site.
export {
  LayoutDashboard as DashboardIcon,
  Users as CustomersIcon,
  Package as ProductsIcon,
  Receipt as ChallansIcon,
  Search as SearchIcon,
  Plus as PlusIcon,
  LogOut as LogoutIcon,
  TriangleAlert as WarningIcon,
} from "lucide-react";
