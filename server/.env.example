# Supabase Postgres connection pooler (used at runtime by the app)
DATABASE_URL=

# Supabase direct connection, port 5432 (used by Prisma Migrate)
DIRECT_URL=

# Secret used to sign/verify JWTs - use a long random string, never commit the real value
JWT_SECRET=

PORT=
